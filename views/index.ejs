<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css" />
    <title>MVC Clase Digital House</title>
</head>

<body class="container">
    <div class="row justify-content-start">
        <header class="col-md-3 col-12">
            <nav class="row">
                <form class="my-3 col-6 col-md-12 " method="post"
                    action="changeSlide/?slide=<%= slidesNumber.previousSlideNumber %>">
                    <button class=" btn btn-danger"
                        <%= slidesNumber.previousSlideNumber ? 'type="submit"' : "disabled" %>>Volver a
                        diapositiva
                        <%= slidesNumber.previousSlideNumber %></button>
                </form>
                <form class="hide-sm" method="post">
                    <ul class="indexList">
                        <% for(let i of index) {%>
                        <li class="mt-1">
                            <button
                                class="btn  <%= i.id == slidesNumber.currentSlideNumber ? "btn-warning" : "btn-secondary" %>"
                                type="submit" formaction="changeSlide/?slide=<%= i.id %>"><%= i.title %> </button>
                        </li>
                        <% } %>
                    </ul>
                </form>
                <form class="my-3 my-md-0 col-6 col-md-12" method="post"
                    action="changeSlide/?slide=<%= slidesNumber.nextSlide %>">
                    <button class="btn btn-danger" <%= slidesNumber.nextSlide ? 'type="submit"' : "disabled" %>>Seguir
                        a
                        diapositiva
                        <%= slidesNumber.nextSlide %></button>
                </form>
            </nav>
        </header>
        <main class="col-md-9 col-12 p-5 text-white">
            <h1><%= title %> </h1>
            <hr class="mb-3">
            <section class="row"><%- section %> </section>
            <% if(slidesNumber.currentSlideNumber == index.length) {%>
            <form class="position-absolute bottom-0 end-0 p-5" action="/closeProject" method="post">
                <button class="btn btn-danger" type="submit">Cerrar proyecto</button>
            </form>
            <%}%>
        </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>